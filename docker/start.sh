#!/bin/bash

echo "🚀 بدء تشغيل تطبيق الفلكلور..."

# ============================================
# 1. انتظار قاعدة البيانات
# ============================================
echo "⏳ انتظار قاعدة البيانات..."
sleep 5

# ============================================
# 2. تشغيل الهجرات والبذور
# ============================================
echo "📦 تشغيل الهجرات..."
php artisan migrate --force

echo "🌱 تشغيل البذور..."
php artisan db:seed --force

echo "🔐 إعادة تعيين كاش الصلاحيات..."
php artisan permission:cache-reset

# ============================================
# 3. تحسين الأداء
# ============================================
echo "⚡ تحسين الأداء..."
php artisan config:cache
php artisan route:cache
php artisan view:cache

echo "🔗 إنشاء رابط التخزين..."
php artisan storage:link

echo "✅ اكتمل الإعداد بنجاح!"

# ============================================
# 4. تشغيل الخدمات
# ============================================
echo "🌐 تشغيل Nginx..."
service nginx start

echo "🐘 تشغيل PHP-FPM (foreground)..."
# ✅ -F تعني foreground - تُبقي العملية قيد التشغيل
php-fpm -F